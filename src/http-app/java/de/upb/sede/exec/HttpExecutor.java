package de.upb.sede.exec;

public class HttpExecutor extends Executor{

	public HttpExecutor(ExecutorConfiguration execConfig, ExecutionPool execPool) throws Exception {
		super(execConfig, execPool);
	}
}
